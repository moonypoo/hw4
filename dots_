function setup() {
  createCanvas(400, 400);
  colorMode(HSB, width, height, 100);
}

function draw() {
  var y = random(height);
  for (var i = 0; i < 1000; i++) {
    var x = random(width);


    stroke(x, y, 100);
    point(x, y);
  }
}
